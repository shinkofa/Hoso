package com.theermite.hoso.config

/**
 * Public legal links surfaced in the UI.
 *
 * Centralised here so the privacy policy can move (GitHub file → GitHub
 * Pages → theermite.com) by editing a single constant — the Play Console
 * listing URL is updated separately.
 */
object LegalLinks {
    const val PRIVACY_POLICY_URL =
        "https://github.com/shinkofa/Hoso/blob/main/PRIVACY.md"
}
