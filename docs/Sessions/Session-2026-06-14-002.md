# Session 2026-06-14 #2 — Phases B + C + D publication Play Store

> Projet : Hoso (app streaming Android perso, Apache 2.0)
> Durée : 2026-06-14 11:26 → 2026-06-15 01:00 (Europe/Madrid)
> Continuité : suite de Session-2026-06-14-001 (Phase A, score 88)
> Mode : plan par phase (B, C, D planifiées et validées séparément)

## Travail réalisé

### Phase B — conformité build (`230fa9d`, `d88a921`)

Veille décisive (2026-06-14, developer.android.com) : le RTMP de StreamPack
passe par des **sockets TCP bruts**, non gouvernés par `usesCleartextTraffic`
ni `network_security_config`. Retirer le cleartext ne casse donc pas le stream.

- **`230fa9d`** — retrait de `android:usesCleartextTraffic="true"` du manifest.
  Test manifest étendu (`should_not_enable_global_cleartext_traffic`, rouge avant
  / vert après). Marqueur `[ROBUSTNESS]` fourni.
- **`d88a921`** — bump version `0.1.0`→`1.0.0`, versionCode `1`→`2` (le 1 est pris
  par l'APK GitHub déjà distribué). Décision Jay.
- Vérifié : icône adaptive déjà conforme (rien à faire), signing release déjà câblé.
- Build release prouvé : `assembleRelease` + `bundleRelease` passent, R8 ne casse
  pas StreamPack, APK/AAB signés avec la clé release (`CN=The Ermite, O=Shinkofa`,
  scheme v2). Vérif device (stream sur OPPO) déléguée à Jay.

### Phase C — légal (`b1b6d59`, `455af1c`)

Exploration : l'app **ne collecte rien** (zéro analytics/télémétrie/crash reporting ;
GlitchTip décidé mais pas implémenté). Stockage local only. Privacy story très propre.

- **`b1b6d59`** — `PRIVACY.md` racine repo, trilingue FR/EN/ES. Reflète la réalité :
  aucune collecte, aucun partage tiers, clé de stream locale envoyée seulement à
  l'ingest RTMP choisi par l'utilisateur, rationale des permissions, suppression
  des données, contact via repo.
- **`455af1c`** — lien privacy in-app (exigé par Google pour permissions sensibles).
  Ligne pied de page `MainActivity` → ouvre l'URL. URL centralisée dans un objet
  `LegalLinks` (testable), garde-fou JVM (https + non vide, rouge→vert). VersionName
  lu via PackageManager (pas de BuildConfig à activer).
- Doc de prép Play Console → Obsidian `02-Hoso/Legal/Play-Store-Phase-C.md` :
  réponses Data safety, déclarations FGS (mediaProjection + microphone), **script
  vidéo démo**, classification contenu, note reviewer (pas de login).

### Phase D — assets store (`d6d3b85`, `b3dbd23`)

- **`d6d3b85`** — icône 512×512 dérivée du `Logo-Hoso-fnl.png` (downscale Lanczos,
  fond navy aplati).
- **`b3dbd23`** — feature graphic 1024×500 + 6 captures 9:16 HD (1205×2143).
  **Correctif majeur** : les mockups GitHub montraient Streamer.bot (retiré Phase A).
  4 SVG sources corrigés (retrait cartes/pastilles Streamer.bot), caret déroulant
  `▾`→`▼` (tofu cairosvg), kanji `放送` retiré du hero (pas de police CJK). Re-rendu
  HD via cairosvg, feature graphic recomposé (bannière + 2 téléphones actuels).
- Doc de prép → Obsidian `02-Hoso/Store/Play-Store-Phase-D.md` : descriptions
  FR/EN/ES (courte + longue), specs visuels, état.

### GitHub — mise à jour cohérence (`c86b4a3`, `7c1c544`)

- **`c86b4a3`** — README : retrait des 3 références Streamer.bot (section "pont PC",
  ligne table "Bot bridge", nœud `StreamerBotService` de l'arbre d'archi). Mockups
  PNG du README régénérés depuis les SVG corrigés.
- **`7c1c544`** — suppression de 2 PNG cassés (`png$1.png`, `png$name.png`, exports
  ratés non référencés). Demande Jay.

### AAB final

Clean `bundleRelease` → `app/build/outputs/bundle/release/app-release.aab` (4.0 Mo,
v1.0.0, signé clé release valide jusqu'en 2053). Prêt pour upload Play Console.

## Tests

- Suite JVM (`testDebugUnitTest`) : **verte** en fin de session.
- Nouveaux tests : `should_not_enable_global_cleartext_traffic` (manifest),
  `LegalLinksTest.privacy_policy_url_is_https_and_non_blank`. Les deux en TDG
  (rouge avant implémentation).

## Erreurs rencontrées + corrections

| Erreur | Cause | Correction |
|--------|-------|------------|
| Mockups Play Store montraient Streamer.bot | SVG sources non mis à jour après retrait Phase A | 4 SVG corrigés + re-rendus ; détecté à la QA visuelle avant publication |
| README texte + images obsolètes (Streamer.bot) | Idem, jamais nettoyé | Section/table/archi retirées + PNG régénérés |
| Caret déroulant `▾` en tofu (carré vide) | cairosvg manque le glyphe U+25BE | Remplacé par `▼` (U+25BC, rendu OK) |
| Kanji `放送` en tofu sur le hero | Pas de police CJK dans cairosvg | Retiré du hero (logo "Hōsō" suffit) |

## Décisions

- versionName `1.0.0` / versionCode `2` pour la première soumission (Jay).
- Cleartext retiré tout court (pas de network_security_config) — sans effet sur socket RTMP brut.
- Hébergement privacy policy : fichier repo GitHub (migration Pages/site possible plus tard).
- Langues privacy + descriptions : trilingue FR/EN/ES.
- Visuels : mockups SVG corrigés (refaits proprement par Takumi, auteur d'origine) ;
  vraies captures device = amélioration future optionnelle.

## Métriques objectives

- Commits poussés (travail Takumi) : **8** (`230fa9d`, `d88a921`, `b1b6d59`,
  `455af1c`, `d6d3b85`, `b3dbd23`, `c86b4a3`, `7c1c544`). Tous sur `origin/main`.
- Unpushed : **0**. TODO/FIXME ajoutés : **0**.
- Marqueurs veille : 1 `[VEILLE]` Android cleartext + 2 `[VEILLE-SKIP]` (édits méthodo/assets).
- Context : reprise post-compaction en début de session ; pas de saturation notable.
  Plusieurs rappels hook simple-language (delivery) corrigés en cours de route.

## Context Awareness

Seuil 60 % non atteint de façon critique. Aucune dégradation circulaire. Session
longue (~13 h calendaires avec pauses) mais cache prompt 1 h a maintenu la continuité.

## Pending — prochaine session (Phase E, côté Jay)

Compte Play Console créé + vérifié, **fiche app pas encore créée**. Ordre :

1. Créer la fiche de l'app (nom, catégorie, langue par défaut).
2. Tester le stream release sur OPPO CPH2173 + confirmer (cleartext retiré + R8).
3. Enregistrer la vidéo démo FGS (script dans guide Phase C).
4. Remplir formulaires (Data safety, FGS, classification, app access) + uploader
   AAB + icône + feature graphic + 6 captures + descriptions + URL privacy.
5. Renseigner email contact (Confidentiality — pas par Takumi).
6. Soumettre pour revue.

Guides : `02-Hoso/Legal/Play-Store-Phase-C.md` + `02-Hoso/Store/Play-Store-Phase-D.md`.

## Scoring V2

| Dimension | Score | Notes |
|-----------|-------|-------|
| Value | 90 | 3 phases livrées (B/C/D) : app conforme, privacy policy, tous les visuels, AAB final, GitHub propre. Déblocage publication majeur. |
| Reliability | 88 | Mockups Streamer.bot périmés rattrapés à la QA avant publication (zéro régression livrée) ; tofu glyphes corrigés ; 0 rework livré. |
| Process | 90 | TDG sur les 2 changements logiques, commits atomiques, veille tracée, reformulations, plan par phase. Quelques warns delivery (simple-language). |
| **Total** | **89** | (90×0.4)+(88×0.3)+(90×0.3) = 36+26.4+27 |

## Visibilité

Candidat article confirmé (déjà noté Phase A) : "specialUse vs binding au FGS d'un
autre service" — cas Hoso. Nouveau candidat : "Pourquoi retirer `usesCleartextTraffic`
ne casse PAS un stream RTMP (sockets bruts hors scope des flags réseau Android)" —
insight veille concret et contre-intuitif. À noter dans Contenu.md.
